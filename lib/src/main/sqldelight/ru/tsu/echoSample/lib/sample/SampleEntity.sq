CREATE TABLE SampleEntity (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL
);

postSample:
INSERT OR REPLACE INTO SampleEntity(name)
VALUES (?);

getSampleByName:
SELECT * FROM SampleEntity
WHERE name = :name;